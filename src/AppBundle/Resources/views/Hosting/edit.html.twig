{% extends '::base.html.twig' %}

{% block title %}Edit Hosting{% endblock %}

{% block body %}
    {{ form(edit_form) }}
    <div class="row">
        <div class="form-group">
            <div class="col-sm-2"></div>
            <div class="col-sm-10">
                {{ form_widget(delete_form.submit,{'attr':{'onclick':'return confirm("Are you sure you want to delete?")'}}) }}
            </div>
        </div>
    </div>
{% endblock %}
