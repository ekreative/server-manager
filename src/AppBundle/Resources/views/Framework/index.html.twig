{% extends '::base.html.twig' %}

{% block title %}Frameworks{% endblock %}

{% block headerPanel %}
    <div class="pull-right">
        <a href="{{ path('framework_new') }}" class="btn-default btn">
            Create a new entry
        </a>
    </div>
{% endblock headerPanel %}

{% block body %}
    <table class="table table-stripped va">
        <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Current Version</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tfoot>
        <tr>
            <td colspan="100">
                {{ knp_pagination_render(entities) }}
            </td>
        </tr>
        </tfoot>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('framework_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.currentVersion }}</td>
                <td>
                    <div class="btn-group">
                        <a href="{{ path('framework_show', { 'id': entity.id }) }}" class="btn-default btn">show</a>

                        <a href="{{ path('framework_edit', { 'id': entity.id }) }}" class="btn-default btn">edit</a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


    <a href="{{ path('framework_new') }}" class="btn-default btn">
        Create a new entry
    </a>

{% endblock %}
